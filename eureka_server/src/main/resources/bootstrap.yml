spring:
  cloud:
    config:
      uri: http://localhost:20000
      name: application
      profile: eurekaserver
  application:
    name: eureka-server
server:
  port: 11000
eureka:
  client:
    fetch-registry: false

